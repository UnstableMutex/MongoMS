<UserControl x:Class="MongoMS.View.AddDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="115*"/>
            <RowDefinition Height="147*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox TextWrapping="Wrap"  Text="{Binding Document, Delay=100, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>
        <Button Command="{Binding AddDocumentCommand}" Content="ok" Margin="4" Padding="10,3" HorizontalAlignment="Right"  Grid.Row="2" />
        <Button Content="guidid" Command="{Binding AddGuidIdCommand}" HorizontalAlignment="Left" Margin="26,22,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</UserControl>
